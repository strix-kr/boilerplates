import Layout from './Layout.vue';
import NotFound from './NotFound.vue';
import Login from './Login.vue';

export {
  // Header, Footer, Navigation, Container를 가지는 레이아웃 페이지에서는 Layout을 사용합니다.
  Layout,

  // route의 결과가 없을 때 NotFound를 사용합니다.
  NotFound,
  Login,
};
